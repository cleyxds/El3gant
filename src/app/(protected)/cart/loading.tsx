import Container from "@mui/material/Container"
import Typography from "@mui/material/Typography"

export default function Loading() {
  return (
    <Container>
      <Typography variant="body1" color="rgb(240, 240, 240)">
        Carregando...
      </Typography>
    </Container>
  )
}
